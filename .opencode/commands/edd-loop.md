---
description: "Run the canonical EDD loop through the primary build agent."
---

Load and use the canonical `edd-loop` skill from
`~/.config/opencode/skills/edd-loop/SKILL.md`. Follow its contracts and
handoffs; this command is only an OpenCode adapter and does not duplicate the
protocol. Complete the contract's client/model/permission preflight before any
edit or worker/verifier call.

Primary build-agent request:

`$ARGUMENTS`
